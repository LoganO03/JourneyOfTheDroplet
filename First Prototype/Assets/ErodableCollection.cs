using UnityEngine;

public class ErodableCollection : MonoBehaviour
{
    [SerializeField] public Texture2D Rocks; // visual texture
    [SerializeField] public Texture2D Voronoi; // rock lookup map
    
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
